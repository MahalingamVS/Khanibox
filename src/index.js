import React from 'react';
import ReactDOM from 'react-dom';
import PreviewCorrecrResponse from './display-preview/PreviewCorrecrResponse'

ReactDOM.render(
  <React.StrictMode>
    <PreviewCorrecrResponse />
  </React.StrictMode>,
  document.getElementById('root')
);